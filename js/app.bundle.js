/* Generated by scripts/build-offline.mjs. Do not edit directly. */

// Source: data/content.js
const learningStops = Object.freeze([
  {
    id: "contrast",
    number: "01",
    title: "Framed Vastness",
    principle: "Contrast and Balance",
    chinese: "对比与平衡",
    phrase: "Small opening, boundless scene",
    interaction: "Open the lattice window",
    insight: "A limited opening makes the world beyond it feel larger.",
    intro: "Openings and restraint let a compact garden suggest a larger world.",
    principles: [
      { english: "Various scales of openings", chinese: "大小开合" },
      { english: "Vastness within a small area", chinese: "小中见大" },
      { english: "Enhance the grand with the small", chinese: "以小衬大" },
      { english: "Less is more", chinese: "以少胜多" },
    ],
    observation: "观景提示：留意漏窗如何框取远景，让有限庭院显出深远与开阔。",
    hotspot: { marker: "窗", label: "点击漏窗阅览对比与平衡 / Open the framed-window lesson" },
    script: "Notice the small window and the wide scene beyond it. In a Chinese classical garden, restraint can make space feel vast. This is contrast and balance.",
  },
  {
    id: "poetic",
    number: "02",
    title: "A Walkable Painting",
    principle: "Poetic and Artistic Refinement",
    chinese: "诗画的雅趣",
    phrase: "A painting entered on foot",
    interaction: "Wake the reflection",
    insight: "A garden invites us to walk inside a painting and a poem.",
    intro: "Architecture, names and inscriptions make a garden a walkable poem and painting.",
    principles: [
      { english: "3D landscape painting", chinese: "三维山水画" },
      { english: "A reflection of the literati's personality and worldview", chinese: "造园人的文人情结" },
      { english: "Residences and social hangouts", chinese: "文人雅士的雅集文酒之处" },
      { english: "Poetic names of architecture", chinese: "亭台楼阁的诗意命名" },
      { english: "Inscriptions of poetry or calligraphic art on the plaques and gate couplets", chinese: "匾额的书法题诗与楹联" },
      { english: "Ubiquitous cultural symbols", chinese: "无处不在的文化符号" },
    ],
    observation: "观景提示：看看水亭与匾额所在之处，建筑如何成为诗文、书法与雅集的载体。",
    hotspot: { marker: "诗", label: "点击亭上匾额阅览诗画的雅趣 / Open the pavilion-plaque lesson" },
    script: "Reflection, a poetic name, and quiet architecture turn the garden into a three-dimensional landscape painting. This is poetic and artistic refinement.",
  },
  {
    id: "harmony",
    number: "03",
    title: "Changing Views",
    principle: "Diversity and Harmony",
    chinese: "多样性与和谐性",
    phrase: "One turn, another scene",
    interaction: "Follow the winding path",
    insight: "Every step can open another view without losing harmony.",
    intro: "Variety unfolds step by step, held together by rhythm, concealment and calm.",
    principles: [
      { english: "Winding paths to seclusion", chinese: "曲径通幽" },
      { english: "Interplay of light and shadow", chinese: "明暗交错" },
      { english: "Alternation of density and spacing", chinese: "疏密相间" },
      { english: "Balance between openness and privacy", chinese: "旷奥相济" },
      { english: "Changing views with changing steps", chinese: "移步换景" },
      { english: "Beauty in all four seasons", chinese: "四季皆景" },
      {
        english: "Nuanced but profound aesthetics",
        chinese: "含蓄幽远",
        children: [
          { english: "Concealment", chinese: "犹抱琵琶" },
          { english: "Multiple layers", chinese: "错落有致" },
          { english: "Empty Space", chinese: "留白艺术" },
        ],
      },
    ],
    observation: "观景提示：顺着曲径观察竹影、遮掩、层次和水面留白，景色如何逐步显现。",
    hotspot: { marker: "径", label: "点击曲径阅览多样性与和谐性 / Open the winding-path lesson" },
    script: "A winding path hides and reveals. Light, shadow, layers, and empty space create variety while the whole garden remains harmonious.",
  },
  {
    id: "naturalness",
    number: "04",
    title: "Made to Feel Natural",
    principle: "Naturalness and Simplicity",
    chinese: "自然与质朴",
    phrase: "Craft resting in nature",
    interaction: "Lift the morning mist",
    insight: "Craft is most graceful when it seems to follow nature.",
    intro: "Good design settles into terrain and water until craft feels natural.",
    principles: [
      { english: "Tailor to the natural environment", chinese: "因地制宜" },
      { english: "Nestled by/follow mountains and waters", chinese: "依山傍水" },
      { english: "Unity between nature and man", chinese: "天人合一" },
      { english: "Retreat to serene nature", chinese: "归隐山林" },
    ],
    observation: "观景提示：观察临水亭台与叠石竹木的关系，人造之景如何顺应水岸与地势。",
    hotspot: { marker: "山", label: "点击水岸叠石阅览自然与质朴 / Open the waterside lesson" },
    script: "Here, buildings do not dominate mountain and water. Human design settles quietly into the landscape. This is naturalness and simplicity.",
  },
]);

const questions = Object.freeze([
  {
    id: "frame",
    lens: "contrast",
    prompt: "Choose the view that feels most expansive.",
    guidance: "Look at how an opening shapes distance.",
    options: [
      { id: "frameA", label: "A framed glimpse beyond the wall", correct: true },
      { id: "frameB", label: "A fully exposed view", correct: false },
    ],
    rationale: "A restrained frame lets a modest garden feel surprisingly vast.",
  },
  {
    id: "naming",
    lens: "poetic",
    prompt: "Name the pavilion that completes the mood.",
    guidance: "Which name lets water and light become poetry?",
    scene: "poetic",
    options: [
      { id: "bright-echoes", label: "Hall of Bright Echoes", correct: true },
      { id: "meeting-room", label: "Meeting Room", correct: false },
      { id: "waterfront-building", label: "Large Waterfront Building", correct: false },
    ],
    rationale: "A poetic name lets architecture continue the feeling of water and light.",
  },
  {
    id: "path",
    lens: "harmony",
    prompt: "Guide the visitor's path.",
    guidance: "A garden is discovered step by step.",
    options: [
      { id: "pathA", label: "Winding reveal through bamboo", correct: true },
      { id: "pathB", label: "Direct exposure across the pond", correct: false },
    ],
    rationale: "Concealment and reveal allow changing views to remain harmonious.",
  },
  {
    id: "placement",
    lens: "naturalness",
    prompt: "Place one final pavilion.",
    guidance: "Let human craft settle into mountain and water.",
    options: [
      { id: "placementA", label: "Nested quietly beside the shore", correct: true },
      { id: "placementB", label: "Dominating the entire pond", correct: false },
    ],
    rationale: "The most graceful structure seems to follow terrain and water.",
  },
  {
    id: "empty-space",
    lens: "harmony",
    prompt: "Which view leaves room for imagination?",
    guidance: "Notice what is concealed and what is left open.",
    options: [
      { id: "emptySpaceA", label: "Layers, concealment and open water", correct: true },
      { id: "emptySpaceB", label: "Every corner filled and explained", correct: false },
    ],
    rationale: "What remains hidden or empty allows the mind to complete the scene.",
  },
]);

const resultProfiles = Object.freeze({
  contrast: {
    title: "The Framing Observer",
    chinese: "借景知音",
    feedback: "You notice how a small frame can hold a wide emotional world.",
  },
  poetic: {
    title: "The Poetic Wanderer",
    chinese: "诗境游人",
    feedback: "You listen for the poem within reflections, names and quiet structures.",
  },
  harmony: {
    title: "The Changing-View Seeker",
    chinese: "移步寻景者",
    feedback: "You value layers, pauses and views that unfold gradually.",
  },
  naturalness: {
    title: "The Quiet Harmonist",
    chinese: "天人和游者",
    feedback: "You sense when human craft belongs peacefully within nature.",
  },
});

// Source: data/assets.js
const images = Object.freeze({
  hero: "./assets/images/hero-moon-gate.jpg",
  contrast: "./assets/images/learn-framed-vastness.jpg",
  poetic: "./assets/images/learn-poetic-reflection.jpg",
  harmony: "./assets/images/learn-changing-view.jpg",
  naturalness: "./assets/images/learn-naturalness.jpg",
  frameA: "./assets/images/play-frame-a.jpg",
  frameB: "./assets/images/play-frame-b.jpg",
  pathA: "./assets/images/play-path-a.jpg",
  pathB: "./assets/images/play-path-b.jpg",
  placementA: "./assets/images/play-placement-a.jpg",
  placementB: "./assets/images/play-placement-b.jpg",
  emptySpaceA: "./assets/images/play-empty-space-a.jpg",
  emptySpaceB: "./assets/images/play-empty-space-b.jpg",
  result: "./assets/images/result-complete-garden.jpg",
});

const narration = Object.freeze({
  contrast: "./assets/audio/narration/contrast.m4a",
  poetic: "./assets/audio/narration/poetic.m4a",
  harmony: "./assets/audio/narration/harmony.m4a",
  naturalness: "./assets/audio/narration/naturalness.m4a",
});

const music = Object.freeze({
  bgm: "./assets/audio/bgm.mp3",
});

// Source: js/state.js
const REQUIRED_STOPS = Object.freeze(["contrast", "poetic", "harmony", "naturalness"]);
const LENSES = Object.freeze(["contrast", "poetic", "harmony", "naturalness"]);
const QUESTION_COUNT = 5;

function createJourneyState() {
  return {
    phase: "arrival",
    activeStop: 0,
    visitedStops: [],
    questionIndex: 0,
    answers: {},
    scores: { contrast: 0, poetic: 0, harmony: 0, naturalness: 0 },
  };
}

function visitStop(state, id) {
  if (!REQUIRED_STOPS.includes(id)) {
    throw new Error("Unknown learning stop.");
  }

  const visitedStops = state.visitedStops.includes(id)
    ? state.visitedStops
    : [...state.visitedStops, id];

  return {
    ...state,
    phase: "learn",
    activeStop: REQUIRED_STOPS.indexOf(id),
    visitedStops,
  };
}

function canEnterChallenge(state) {
  return REQUIRED_STOPS.every((id) => state.visitedStops.includes(id));
}

function enterChallenge(state) {
  if (!canEnterChallenge(state)) {
    throw new Error("Complete the learning walk before beginning the challenge.");
  }

  return {
    ...state,
    phase: "challenge",
    questionIndex: 0,
    answers: {},
    scores: { contrast: 0, poetic: 0, harmony: 0, naturalness: 0 },
  };
}

function answerQuestion(state, { questionId, lens, correct }) {
  if (state.phase !== "challenge") {
    throw new Error("The challenge is not active.");
  }
  if (state.answers[questionId]) {
    throw new Error("This question is already answered.");
  }
  if (!LENSES.includes(lens)) {
    throw new Error("Unknown aesthetic lens.");
  }

  const answers = { ...state.answers, [questionId]: { lens, correct } };
  const scores = {
    ...state.scores,
    [lens]: state.scores[lens] + (correct ? 1 : 0),
  };
  const questionIndex = state.questionIndex + 1;

  return {
    ...state,
    answers,
    scores,
    questionIndex,
    phase: questionIndex === QUESTION_COUNT ? "result" : "challenge",
  };
}

function getResult(state) {
  if (state.phase !== "result") {
    throw new Error("A result is not yet available.");
  }

  const lens = LENSES.reduce(
    (strongest, key) => (state.scores[key] > state.scores[strongest] ? key : strongest),
    LENSES[0],
  );
  const correct = Object.values(state.answers).filter((answer) => answer.correct).length;

  return { lens, score: state.scores[lens], correct };
}

function resetJourney() {
  return createJourneyState();
}

// Source: js/audio.js
function createAudioController(makeAudio = (src) => new Audio(src)) {
  let voice = null;
  let bgm = null;
  let musicStarted = false;
  let musicUnavailable = false;

  async function playNarration(src) {
    voice?.pause();
    voice = makeAudio(src);
    try {
      await voice.play();
      return true;
    } catch {
      return false;
    }
  }

  function stopNarration() {
    voice?.pause();
  }

  async function startMusic(src) {
    if (!bgm) {
      bgm = makeAudio(src);
      bgm.loop = true;
    }

    musicStarted = true;
    try {
      await bgm.play();
    } catch {
      musicUnavailable = true;
    }
  }

  function toggleMusic() {
    if (!bgm || musicUnavailable) return;

    if (bgm.paused) {
      void bgm.play().catch(() => {
        musicUnavailable = true;
      });
    } else {
      bgm.pause();
    }
  }

  function stopAll() {
    voice?.pause();
    bgm?.pause();
  }

  function status() {
    return {
      musicStarted,
      musicPlaying: Boolean(bgm && !bgm.paused && !musicUnavailable),
      musicUnavailable,
    };
  }

  return {
    playNarration,
    stopNarration,
    startMusic,
    toggleMusic,
    stopAll,
    status,
  };
}

// Source: js/app.js

const STORAGE_KEY = "moonGateJourney";
const nodes = Object.fromEntries(
  ["app", "arrival", "learn", "challenge", "result", "audioDock", "announcement"].map((id) => [
    id,
    document.getElementById(id),
  ]),
);
const audio = createAudioController();
let state = restoreState();
let pendingFeedback = null;
let touchStartX = null;
let scrollOpen = false;

function restoreState() {
  try {
    const stored = JSON.parse(sessionStorage.getItem(STORAGE_KEY));
    if (!stored || !["arrival", "learn", "challenge", "result"].includes(stored.phase)) {
      return createJourneyState();
    }
    return stored;
  } catch {
    return createJourneyState();
  }
}

function saveState() {
  sessionStorage.setItem(STORAGE_KEY, JSON.stringify(state));
}

function announce(text) {
  nodes.announcement.textContent = text;
}

function stopNarration() {
  audio.stopNarration();
  if ("speechSynthesis" in window) {
    window.speechSynthesis.cancel();
  }
}

function showPhase(phase) {
  nodes.app.dataset.phase = phase;
  ["arrival", "learn", "challenge", "result"].forEach((name) => {
    nodes[name].hidden = name !== phase;
  });
}

function applyParallax(container) {
  container.onpointermove = (event) => {
    if (matchMedia("(prefers-reduced-motion: reduce)").matches) return;
    const x = (event.clientX / window.innerWidth - .5) * -10;
    const y = (event.clientY / window.innerHeight - .5) * -6;
    container.style.setProperty("--drift-x", `${x}px`);
    container.style.setProperty("--drift-y", `${y}px`);
  };
  container.onpointerleave = () => {
    container.style.removeProperty("--drift-x");
    container.style.removeProperty("--drift-y");
  };
}

function renderArrival() {
  nodes.arrival.innerHTML = `
    <img class="scene-image" src="${images.hero}" alt="">
    <article class="arrival-copy">
      <p class="eyebrow">An English Cultural Appreciation Journey</p>
      <h1 id="journeyTitle">Moon Gate<br>Journey <small>月洞门游园</small></h1>
      <p class="subtitle">Learn to see a Chinese classical garden.</p>
      <button class="seal-button" id="enterGarden">Enter the Garden</button>
      <span class="duration">A six-minute visual journey</span>
    </article>`;

  document.getElementById("enterGarden").addEventListener("click", () => {
    state = visitStop(state, learningStops[0].id);
    saveState();
    render();
  });
  applyParallax(nodes.arrival);
}

async function playNarration(stop, card) {
  card.classList.add("is-listening");
  const played = await audio.playNarration(narration[stop.id]);
  if (played) {
    announce(`Listening to ${stop.principle}.`);
    return;
  }

  if ("speechSynthesis" in window) {
    const speech = new SpeechSynthesisUtterance(stop.script);
    speech.lang = "en-US";
    window.speechSynthesis.cancel();
    window.speechSynthesis.speak(speech);
    announce("Using device American English voice for this passage.");
  } else {
    announce("Audio is unavailable. Read the visible transcript.");
  }
}

function renderPrincipleItems(items, nested = false) {
  return `<ul class="${nested ? "principle-children" : "principle-list"}">
    ${items.map((item) => `
      <li>
        <span class="term-zh">${item.chinese}</span>
        <span class="term-en">${item.english}</span>
        ${item.children ? renderPrincipleItems(item.children, true) : ""}
      </li>`).join("")}
  </ul>`;
}

function setScrollOpen(open) {
  scrollOpen = open;
  const scene = document.getElementById("learnScene");
  const hotspot = document.getElementById("learningHotspot");
  const scroll = document.getElementById("knowledgeScroll");
  if (!scene || !hotspot || !scroll) return;
  scene.classList.toggle("is-observed", open);
  scroll.classList.toggle("is-open", open);
  scroll.toggleAttribute("inert", !open);
  scroll.setAttribute("aria-hidden", String(!open));
  hotspot.setAttribute("aria-expanded", String(open));
}

function openStop(id) {
  stopNarration();
  scrollOpen = false;
  state = visitStop(state, id);
  saveState();
  renderLearn();
}

function renderLearn() {
  const stop = learningStops[state.activeStop];
  const visitedCount = state.visitedStops.length;
  nodes.learn.innerHTML = `
    <div class="learn-scene" id="learnScene">
      <img class="scene-image scene-image--${stop.id}" src="${images[stop.id]}" alt="">
      <div class="learning-effect learning-effect--${stop.id}" aria-hidden="true"></div>
    </div>
    <nav class="stop-nav" aria-label="Four garden principles">
      ${learningStops.map((item, index) => `
        <button class="${state.visitedStops.includes(item.id) ? "visited" : ""} ${index === state.activeStop ? "active" : ""}"
          data-stop="${item.id}" aria-current="${index === state.activeStop ? "step" : "false"}">
          <b>${item.number}</b>${item.title}
        </button>`).join("")}
    </nav>
    <button class="learning-hotspot learning-hotspot--${stop.id}" id="learningHotspot"
      aria-controls="knowledgeScroll" aria-expanded="${scrollOpen}" aria-label="${stop.hotspot.label}">
      <b>${stop.hotspot.marker}</b>
      <span>点击阅览<br><small>Tap to learn</small></span>
    </button>
    <aside class="knowledge-scroll ${scrollOpen ? "is-open" : ""}" id="knowledgeScroll"
      aria-label="${stop.principle} / ${stop.chinese}" aria-hidden="${!scrollOpen}"
      ${scrollOpen ? "" : "inert"}>
      <button class="scroll-close" id="scrollClose" aria-label="关闭知识卷轴 / Close learning scroll">×</button>
      <p class="eyebrow">${stop.number} / Four Principles</p>
      <h2>${stop.title}</h2>
      <p class="chinese">${stop.chinese}</p>
      <p class="principle-name">${stop.principle}</p>
      <p class="scroll-intro">${stop.intro}</p>
      ${renderPrincipleItems(stop.principles)}
      <p class="observation">${stop.observation}</p>
      <p class="script">${stop.script}</p>
      <div class="learning-actions">
        <button class="listen-button" id="listen">播放讲解 / Listen</button>
      </div>
      <div class="walk-footer">
        <p class="gate-status">${visitedCount === 4 ? "The garden gate is open." : `${visitedCount} of 4 principles visited`}</p>
        ${canEnterChallenge(state)
          ? `<button class="seal-button" id="beginChallenge">Begin Challenge</button>`
          : `<button class="soft-button" id="nextStop">${state.activeStop < 3 ? "Next View" : "Visit All Views"}</button>`}
      </div>
    </aside>`;

  const scene = document.getElementById("learnScene");
  const scroll = document.getElementById("knowledgeScroll");
  document.getElementById("learningHotspot").addEventListener("click", () => {
    setScrollOpen(!scrollOpen);
    if (scrollOpen) announce(`${stop.chinese}. ${stop.principle}.`);
  });
  document.getElementById("scrollClose").addEventListener("click", () => setScrollOpen(false));
  scene.addEventListener("click", (event) => {
    if (scrollOpen && !event.target.closest(".learning-hotspot")) setScrollOpen(false);
  });
  document.getElementById("listen").addEventListener("click", () => void playNarration(stop, scroll));
  nodes.learn.querySelectorAll("[data-stop]").forEach((button) => {
    button.addEventListener("click", () => openStop(button.dataset.stop));
  });

  const next = document.getElementById("nextStop");
  next?.addEventListener("click", () => {
    const nextIndex = state.activeStop < 3 ? state.activeStop + 1 : 0;
    openStop(learningStops[nextIndex].id);
  });

  const begin = document.getElementById("beginChallenge");
  begin?.addEventListener("click", async () => {
    stopNarration();
    scrollOpen = false;
    audio.stopAll();
    state = enterChallenge(state);
    pendingFeedback = null;
    saveState();
    await audio.startMusic(music.bgm);
    render();
  });

  scene.addEventListener("pointerdown", (event) => {
    touchStartX = event.clientX;
  });
  scene.addEventListener("pointerup", (event) => {
    if (touchStartX === null || Math.abs(event.clientX - touchStartX) < 48) return;
    const direction = event.clientX < touchStartX ? 1 : -1;
    const nextIndex = Math.max(0, Math.min(learningStops.length - 1, state.activeStop + direction));
    openStop(learningStops[nextIndex].id);
    touchStartX = null;
  });
  applyParallax(nodes.learn);
}

function renderAudioDock() {
  const status = audio.status();
  nodes.audioDock.hidden = state.phase !== "challenge";
  if (state.phase !== "challenge") return;

  nodes.audioDock.innerHTML = `
    <button class="record ${status.musicPlaying ? "" : "is-paused"}" id="musicToggle"
      aria-label="${status.musicPlaying ? "Pause garden music" : "Play garden music"}">
      <span>乐</span>
    </button>
    <p class="audio-caption">${status.musicUnavailable ? "Music unavailable" : "Garden Music"}</p>`;
  document.getElementById("musicToggle").addEventListener("click", async () => {
    if (!status.musicStarted) {
      await audio.startMusic(music.bgm);
    } else {
      audio.toggleMusic();
    }
    renderAudioDock();
  });
}

function renderChoice(question, option) {
  if (question.id === "naming") {
    return `<button class="choice choice--name" data-option="${option.id}"><span>${option.label}</span></button>`;
  }
  return `<button class="choice" data-option="${option.id}">
    <img src="${images[option.id]}" alt="">
    <span>${option.label}</span>
  </button>`;
}

function renderChallenge() {
  const question = questions[state.questionIndex];
  nodes.challenge.innerHTML = `
    <header class="challenge-header">
      <div><p class="eyebrow">Complete the Garden</p><h1>A Seeing Eye</h1></div>
      <div class="challenge-count">${state.questionIndex + 1} / ${questions.length}</div>
    </header>
    <section class="question-panel">
      <h2>${question.prompt}</h2>
      <p class="guidance">${question.guidance}</p>
      ${question.scene ? `<figure class="poetic-stage"><img src="${images[question.scene]}" alt=""></figure>` : ""}
      <div class="choices ${question.id === "naming" ? "choices--names" : ""}">
        ${question.options.map((option) => renderChoice(question, option)).join("")}
      </div>
    </section>`;

  nodes.challenge.querySelectorAll("[data-option]").forEach((button) => {
    button.addEventListener("click", () => {
      const option = question.options.find((candidate) => candidate.id === button.dataset.option);
      state = answerQuestion(state, {
        questionId: question.id,
        lens: question.lens,
        correct: option.correct,
      });
      pendingFeedback = { question, option };
      saveState();
      announce(`${option.correct ? "Well observed." : "Look once more in memory."} ${question.rationale}`);
      renderFeedback();
    });
  });
  renderAudioDock();
}

function renderFeedback() {
  const { question, option } = pendingFeedback;
  nodes.challenge.innerHTML = `
    <article class="feedback-card">
      <div class="mark">${option.correct ? "雅" : "观"}</div>
      <p class="eyebrow">${option.correct ? "Well Observed" : "Look More Gently"}</p>
      <h2>${question.prompt}</h2>
      <p class="feedback">${question.rationale}</p>
      <button class="seal-button" id="continue">
        ${state.phase === "result" ? "Reveal My Garden Eye" : "Continue Journey"}
      </button>
    </article>`;
  renderAudioDock();
  document.getElementById("continue").addEventListener("click", () => {
    pendingFeedback = null;
    render();
  });
}

function renderResult() {
  audio.stopAll();
  nodes.audioDock.hidden = true;
  const result = getResult(state);
  const profile = resultProfiles[result.lens];
  nodes.result.innerHTML = `
    <img class="scene-image" src="${images.result}" alt="">
    <article class="result-card">
      <p class="eyebrow">Your Garden Eye · ${result.correct} / ${questions.length}</p>
      <h2>${profile.title}</h2>
      <p class="chinese">${profile.chinese}</p>
      <p class="result-summary">${profile.feedback} Carry this way of seeing into the next garden you visit.</p>
      <div class="petals" aria-label="${result.correct} well observed choices">
        ${questions.map((_, index) => `<span class="petal ${index < result.correct ? "is-open" : ""}"></span>`).join("")}
      </div>
      <div class="result-actions">
        <button class="seal-button" id="replay">Walk Again</button>
        <button class="soft-button" id="review">Review the Four Principles</button>
      </div>
    </article>`;

  document.getElementById("replay").addEventListener("click", () => {
    state = resetJourney();
    sessionStorage.removeItem(STORAGE_KEY);
    render();
  });
  document.getElementById("review").addEventListener("click", () => {
    state = visitStop(resetJourney(), learningStops[0].id);
    saveState();
    render();
  });
  applyParallax(nodes.result);
}

function render() {
  showPhase(state.phase);
  if (state.phase === "arrival") renderArrival();
  if (state.phase === "learn") renderLearn();
  if (state.phase === "challenge") {
    if (pendingFeedback) renderFeedback();
    else renderChallenge();
  }
  if (state.phase === "result") renderResult();
}

render();
